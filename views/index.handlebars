<div class="jumbotron">
  <h1 class="display-4">Welcome to node blog</h1>
  <p class="lead">This is a simple blog powered by Node JS + Express + MongoDB</p>
  <hr class="my-4">
  <p>Here you can read more about the latest technology news .</p>
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="/users/record" role="button">Create an account</a>
  </p>
</div>

<hr>

<h2>Recent posts</h2>

{{#each posts}}
<div class="card mt-4">
    <div class="card-body">    
        <h3>{{title}}</h3>
        <p>{{description}}</p>
        <a href="/post/{{slug}}"><button class="btn btn-info">Read more</button></a>
        <hr>
        <small>Category: {{category.name}}</small><br>
        <small>Publication date: {{date}}</small>
    </div>
</div>
{{else}}
    <h5>No posts</h5>
{{/each}}